Start a new KWS session
=======================

In order to be able to use the Kids Web Services Parent SDK you'll first have to setup a session.

.. code-block:: java

  KWSParent.sdk.setup (MainActivity.this);

Once you've setup your SDK session, it's time to authenticate (or create) a user.

All of of the functionality of the SDK assumes you're logged in.
